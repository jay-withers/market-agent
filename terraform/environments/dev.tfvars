environment = "dev"

# The SQL administrator defaults to whoever runs `terraform apply`. Set these to
# name a specific principal instead — an Entra group is the tidiest choice,
# since it keeps administrator access working no matter who (or what) applies:
#
# sql_admin_object_id      = "00000000-0000-0000-0000-000000000000"
# sql_admin_login_username = "investagent-dev-sql-admins"
